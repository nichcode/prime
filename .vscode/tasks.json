{
    "tasks": [
        {
            "type": "shell",
            "label": "build everything",
            "command": "${workspaceFolder}/scripts/build_debug_make.bat",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}